sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t lehoai257/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true lehoai257/react-test npm test

after_success:
  - docker build -t lehoai257/multi-client ./client
  - docker build -t lehoai257/multi-nginx ./nginx
  - docker build -t lehoai257/multi-server ./server
  - docker build -t lehoai257/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push lehoai257/multi-client
  - docker push lehoai257/multi-nginx
  - docker push lehoai257/multi-server
  - docker push lehoai257/multi-worker
